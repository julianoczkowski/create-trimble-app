---
description: Critical integration patterns for Modus Navbar with Side Navigation in SolidJS
globs: ["**/components/ModusNavbar*.tsx", "**/components/ModusSideNavigation*.tsx", "**/demos/**/page.tsx"]
alwaysApply: false
---

# Modus Navbar + Side Navigation Integration

Critical patterns for integrating `ModusNavbar` with `ModusSideNavigation` in SolidJS applications.

## Key Rules

- **Use the ModusNavbar/ModusSideNavigation wrapper components** - Never use raw web components directly
- **Complex props (userCard, visibility) are set as JS properties** - Wrapper handles this via onMount/createEffect
- **Sync navbar hamburger with side navigation** - Use separate signal state per demo section
- **Menu item icons use the `start-icon` slot** - Pass `<i class="modus-icons" slot="start-icon">name</i>` as a child, NOT as a prop

---

## DO: Sync Navbar Hamburger with Side Navigation

Use signals to keep the hamburger menu and side navigation in sync.

```tsx
const [expanded, setExpanded] = createSignal(false);
const [menuOpen, setMenuOpen] = createSignal(false);

const handleMainMenuOpenChange = (open: boolean) => {
  setMenuOpen(open);
  setExpanded(open);
};

const handleExpandedChange = (exp: boolean) => {
  setExpanded(exp);
  setMenuOpen(exp);
};

<ModusNavbar
  userCard={userCard}
  visibility={{ mainMenu: true, user: true }}
  mainMenuOpen={menuOpen()}
  onMainMenuOpenChange={handleMainMenuOpenChange}
  mainMenuContent={
    <ModusSideNavigation
      items={navItems}
      expanded={expanded()}
      targetContent="#content-id"
      onExpandedChange={handleExpandedChange}
      onItemSelect={(item) => setSelectedItem(item.value)}
    />
  }
/>
```

## DON'T: Share state between multiple demo sections

Each navbar+sidenav pair needs its own independent signals.

```tsx
// DON'T: Two sections sharing the same signal
const [expanded, setExpanded] = createSignal(false);

<Section1> // Uses expanded()
<Section2> // Also uses expanded() - they interfere!

// DO: Separate components with their own state
function Section1() {
  const [expanded, setExpanded] = createSignal(false);
  // ...
}
function Section2() {
  const [expanded, setExpanded] = createSignal(false);
  // ...
}
```

---

## DO: Use Slot-Based Icons in Menu Items

The `modus-wc-menu-item` web component renders icons via a named `<slot>`, not via the `startIcon` property. Setting the property has no visible effect.

```tsx
// DO: Use the start-icon slot with a child element
const navItems: MenuItem[] = [
  { label: "Dashboard", value: "dashboard", startIcon: "dashboard" },
];

// In ModusMenuItem component:
<modus-wc-menu-item attr:label={props.label} attr:value={props.value}>
  <i class="modus-icons" slot="start-icon">{props.startIcon}</i>
</modus-wc-menu-item>
```

```tsx
// DON'T: This will NOT render any icon
<modus-wc-menu-item start-icon="dashboard" label="Dashboard" />
```

---

## DO: Contain Demo Layout

Wrap navbar+sidenav demos in fixed-height containers to prevent page-level layout shifts.

```tsx
<div class="overflow-hidden rounded-lg border-default" style="height: 320px">
  <ModusNavbar ... />
  <div class="flex" style="height: calc(100% - 56px)">
    <div id="content-target" class="flex-1 p-4 bg-card">
      Content area
    </div>
  </div>
</div>
```

---

## DO: Use Correct User Card Structure

```tsx
const userCard = {
  name: "John Doe",
  email: "john.doe@example.com",
  avatarSrc: "",
  avatarAlt: "User Avatar",
};
```

---

## Trimble Logo Assets

The Trimble logo in the navbar loads via Stencil's `getAssetPath()`. Two things are required:

1. **`defineCustomElements` with resourcesUrl** in `src/index.tsx`:
```tsx
defineCustomElements(window, { resourcesUrl: window.location.origin + "/" });
```

2. **Static asset copy** in `vite.config.ts` to both paths:
```tsx
viteStaticCopy({
  targets: [
    { src: "node_modules/@trimble-oss/moduswebcomponents/modus-wc/assets/*", dest: "modus-wc/assets" },
    { src: "node_modules/@trimble-oss/moduswebcomponents/modus-wc/assets/*", dest: "assets" },
  ],
})
```

Without both, logos render as broken images on deep routes like `/dev/demos/side-navigation-demo`.

---

## Common Pitfalls

1. **Menu item icons not showing** - Use `<i class="modus-icons" slot="start-icon">name</i>` child element, not the `startIcon` property
2. **Trimble logo broken on deep routes** - Ensure asset copy targets both `modus-wc/assets` and `assets` directories
3. **Two demo sections interfere** - Use separate components with independent signal state
4. **Side nav expands the page width** - Wrap in `overflow-hidden` container with fixed height
5. **Events not firing** - Use `addEventListener` in `onMount` for web component custom events

---

## Reference Implementation

See `/src/demos/side-navigation-demo/page.tsx` for complete working examples.
